# 根据难度调整的课程学习配置
# 体现"越往后越难"的特点

# 🎯 递增式阈值，后期增幅更大
export CURRICULUM_PERFORMANCE_THRESHOLD_1=0.60  # 基础阶段：相对容易
export CURRICULUM_PERFORMANCE_THRESHOLD_2=0.67  # +0.07
export CURRICULUM_PERFORMANCE_THRESHOLD_3=0.75  # +0.08  
export CURRICULUM_PERFORMANCE_THRESHOLD_4=0.84  # +0.09
export CURRICULUM_PERFORMANCE_THRESHOLD_5=0.94  # +0.10 (最难)

# 📊 最小评估次数：后期需要更多验证
export CURRICULUM_MIN_EVALUATIONS_0=8   # 基础阶段：快速验证
export CURRICULUM_MIN_EVALUATIONS_1=10  # 初级阶段
export CURRICULUM_MIN_EVALUATIONS_2=12  # 中级阶段
export CURRICULUM_MIN_EVALUATIONS_3=15  # 高级阶段：需要更多验证
export CURRICULUM_MIN_EVALUATIONS_4=20  # 专家阶段：严格验证

# 🔄 循环训练：每轮递增幅度
export CURRICULUM_MAX_ROUNDS=4
export CURRICULUM_THRESHOLD_INCREMENT=0.04  # 每轮+0.04，体现难度递增

# 🎲 额外的难度控制
export CURRICULUM_STABILITY_WINDOW=3        # 稳定性窗口
export CURRICULUM_PERFORMANCE_CHECK_INTERVAL=30  # 检查间隔 